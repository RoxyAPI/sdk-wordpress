; Supply-chain cooldown. Skip npm versions published less than 3 days ago.
; Filters smash-and-grab compromises (most malware is yanked within hours).
; Unit is days. Requires npm CLI >= 11.10.0; older npm silently ignores this key.
min-release-age=3
