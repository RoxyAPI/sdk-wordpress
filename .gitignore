/vendor/
/node_modules/
/build/

# Jed script catalogues, one per built bundle per locale. Derived from the PO
# files by `npm run i18n:json` inside build:all, exactly as build/ is, and
# shipped from the working tree by both release paths. A thousand files nobody
# edits do not belong in history.
/languages/*.json

*.log
.phpunit.result.cache
.phpcs.cache
phpstan.cache
.DS_Store
.idea/
.vscode/
.claude/
.cursor/
roxyapi.zip

# Maintainer-only docs, never published or committed
CLAUDE.md
MAINTAINER.md
MAINTAINER-*.md
AUDIT-*.md
todo.md
/docs/
