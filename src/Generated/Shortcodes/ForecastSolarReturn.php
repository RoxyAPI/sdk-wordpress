<?php
/**
 * Auto-generated shortcode: [roxy_forecast_solar_return]
 *
 * Solar return chart - Annual birthday forecast chart for a single subject
 *
 * DO NOT EDIT. Generated by bin/generate.mjs. Edit the generator instead.
 *
 * @package RoxyAPI
 */

namespace RoxyAPI\Generated\Shortcodes;

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

use RoxyAPI\Support\ComponentRenderer;

class ForecastSolarReturn {

	public const TAG = 'roxy_forecast_solar_return';

	/**
	 * Every attribute this shortcode accepts. shortcode_atts() silently
	 * discards anything absent from here, so this is also the contract the
	 * library copy-paste sample is checked against.
	 *
	 * @var array<string, string>
	 */
	public const DEFAULTS = array(
		'date' => '',
		'time' => '',
		'year' => '',
		'latitude' => '',
		'longitude' => '',
		'timezone' => '',
		'house_system' => '',
		'lang' => '',
		'hide_readings' => 'inherit',
		'hide_sections' => 'inherit',
	);

	public static function register(): void {
		if ( shortcode_exists( self::TAG ) ) {
			return;
		}
		add_shortcode( self::TAG, array( self::class, 'render' ) );
	}

	public static function render( $atts, $content = '', $tag = '' ): string {
		$atts = shortcode_atts(
			self::DEFAULTS,
			is_array( $atts ) ? $atts : array(),
			(string) $tag
		);

		wp_enqueue_style( 'roxyapi-frontend' );

				$body = array_filter(
			array(
				'date' => $atts['date'],
				'time' => $atts['time'],
				'year' => $atts['year'] !== '' ? (int) $atts['year'] : '',
				'latitude' => $atts['latitude'] !== '' ? (float) $atts['latitude'] : '',
				'longitude' => $atts['longitude'] !== '' ? (float) $atts['longitude'] : '',
				'timezone' => $atts['timezone'] !== '' ? ( is_numeric( $atts['timezone'] ) ? (float) $atts['timezone'] : $atts['timezone'] ) : '',
				'houseSystem' => $atts['house_system'],
			),
			static function ( $v ) {
				return $v !== '';
			}
		);
		$query = array_filter(
			array(
				'lang' => $atts['lang'],
			),
			static function ( $v ) {
				return $v !== '';
			}
		);
		$data = \RoxyAPI\Generated\Client::forecastSolarReturn( $body, $query );

		if ( is_wp_error( $data ) ) {
			return \RoxyAPI\Support\Templates::api_error( $data );
		}

		return ComponentRenderer::render_atts( 'forecastSolarReturn', is_array( $data ) ? $data : array(), $atts );
	}
}
