<?php
/**
 * Auto-generated shortcode: [roxy_calculate_name_compatibility]
 *
 * Compare two names - gematria name compatibility API with every component published
 *
 * DO NOT EDIT. Generated by bin/generate.mjs. Edit the generator instead.
 *
 * @package RoxyAPI
 */

namespace RoxyAPI\Generated\Shortcodes;

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

use RoxyAPI\Support\ComponentRenderer;

class CalculateNameCompatibility {

	public const TAG = 'roxy_calculate_name_compatibility';

	/**
	 * Every attribute this shortcode accepts. shortcode_atts() silently
	 * discards anything absent from here, so this is also the contract the
	 * library copy-paste sample is checked against.
	 *
	 * @var array<string, string>
	 */
	public const DEFAULTS = array(
		'first_name' => '',
		'first_name_hebrew' => '',
		'second_name' => '',
		'second_name_hebrew' => '',
		'transliteration' => '',
		'mispar_gadol' => '',
		'lang' => '',
		'hide_readings' => 'inherit',
		'hide_sections' => 'inherit',
	);

	public static function register(): void {
		if ( shortcode_exists( self::TAG ) ) {
			return;
		}
		add_shortcode( self::TAG, array( self::class, 'render' ) );
	}

	public static function render( $atts, $content = '', $tag = '' ): string {
		$atts = shortcode_atts(
			self::DEFAULTS,
			is_array( $atts ) ? $atts : array(),
			(string) $tag
		);

		wp_enqueue_style( 'roxyapi-frontend' );

				$body = array_filter(
			array(
				'firstName' => $atts['first_name'],
				'firstNameHebrew' => $atts['first_name_hebrew'],
				'secondName' => $atts['second_name'],
				'secondNameHebrew' => $atts['second_name_hebrew'],
				'transliteration' => $atts['transliteration'],
				'misparGadol' => $atts['mispar_gadol'],
			),
			static function ( $v ) {
				return $v !== '';
			}
		);
		$query = array_filter(
			array(
				'lang' => $atts['lang'],
			),
			static function ( $v ) {
				return $v !== '';
			}
		);
		$data = \RoxyAPI\Generated\Client::calculateNameCompatibility( $body, $query );

		if ( is_wp_error( $data ) ) {
			return \RoxyAPI\Support\Templates::api_error( $data );
		}

		return ComponentRenderer::render_atts( 'calculateNameCompatibility', is_array( $data ) ? $data : array(), $atts );
	}
}
