<?php
/**
 * Auto-generated shortcode: [roxy_calculate_gematria]
 *
 * Calculate gematria - Hebrew gematria calculator API with every spelling shown
 *
 * DO NOT EDIT. Generated by bin/generate.mjs. Edit the generator instead.
 *
 * @package RoxyAPI
 */

namespace RoxyAPI\Generated\Shortcodes;

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

use RoxyAPI\Support\ComponentRenderer;

class CalculateGematria {

	public const TAG = 'roxy_calculate_gematria';

	/**
	 * Every attribute this shortcode accepts. shortcode_atts() silently
	 * discards anything absent from here, so this is also the contract the
	 * library copy-paste sample is checked against.
	 *
	 * @var array<string, string>
	 */
	public const DEFAULTS = array(
		'text' => '',
		'text_hebrew' => '',
		'transliteration' => '',
		'ciphers' => '',
		'mispar_gadol' => '',
		'atbash_output' => '',
		'include_matches' => '',
		'latin_ciphers' => '',
		'lang' => '',
		'hide_readings' => 'inherit',
		'hide_sections' => 'inherit',
	);

	public static function register(): void {
		if ( shortcode_exists( self::TAG ) ) {
			return;
		}
		add_shortcode( self::TAG, array( self::class, 'render' ) );
	}

	public static function render( $atts, $content = '', $tag = '' ): string {
		$atts = shortcode_atts(
			self::DEFAULTS,
			is_array( $atts ) ? $atts : array(),
			(string) $tag
		);

		wp_enqueue_style( 'roxyapi-frontend' );

				$body = array_filter(
			array(
				'text' => $atts['text'],
				'textHebrew' => $atts['text_hebrew'],
				'transliteration' => $atts['transliteration'],
				'ciphers' => $atts['ciphers'],
				'misparGadol' => $atts['mispar_gadol'],
				'atbashOutput' => $atts['atbash_output'],
				'includeMatches' => $atts['include_matches'] !== '' ? filter_var( $atts['include_matches'], FILTER_VALIDATE_BOOLEAN ) : '',
				'latinCiphers' => $atts['latin_ciphers'] !== '' ? filter_var( $atts['latin_ciphers'], FILTER_VALIDATE_BOOLEAN ) : '',
			),
			static function ( $v ) {
				return $v !== '';
			}
		);
		$query = array_filter(
			array(
				'lang' => $atts['lang'],
			),
			static function ( $v ) {
				return $v !== '';
			}
		);
		$data = \RoxyAPI\Generated\Client::calculateGematria( $body, $query );

		if ( is_wp_error( $data ) ) {
			return \RoxyAPI\Support\Templates::api_error( $data );
		}

		return ComponentRenderer::render_atts( 'calculateGematria', is_array( $data ) ? $data : array(), $atts );
	}
}
