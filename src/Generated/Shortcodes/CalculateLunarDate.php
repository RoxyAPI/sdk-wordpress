<?php
/**
 * Auto-generated shortcode: [roxy_calculate_lunar_date]
 *
 * Convert lunar and Gregorian dates - Chinese lunisolar calendar API
 *
 * DO NOT EDIT. Generated by bin/generate.mjs. Edit the generator instead.
 *
 * @package RoxyAPI
 */

namespace RoxyAPI\Generated\Shortcodes;

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

use RoxyAPI\Support\ComponentRenderer;

class CalculateLunarDate {

	public const TAG = 'roxy_calculate_lunar_date';

	/**
	 * Every attribute this shortcode accepts. shortcode_atts() silently
	 * discards anything absent from here, so this is also the contract the
	 * library copy-paste sample is checked against.
	 *
	 * @var array<string, string>
	 */
	public const DEFAULTS = array(
		'date' => '',
		'lunar_year' => '',
		'lunar_month' => '',
		'lunar_day' => '',
		'is_leap_month' => '',
		'lang' => '',
		'hide_readings' => 'inherit',
		'hide_sections' => 'inherit',
	);

	public static function register(): void {
		if ( shortcode_exists( self::TAG ) ) {
			return;
		}
		add_shortcode( self::TAG, array( self::class, 'render' ) );
	}

	public static function render( $atts, $content = '', $tag = '' ): string {
		$atts = shortcode_atts(
			self::DEFAULTS,
			is_array( $atts ) ? $atts : array(),
			(string) $tag
		);

		wp_enqueue_style( 'roxyapi-frontend' );

				$body = array_filter(
			array(
				'date' => $atts['date'],
				'lunarYear' => $atts['lunar_year'] !== '' ? (int) $atts['lunar_year'] : '',
				'lunarMonth' => $atts['lunar_month'] !== '' ? (int) $atts['lunar_month'] : '',
				'lunarDay' => $atts['lunar_day'] !== '' ? (int) $atts['lunar_day'] : '',
				'isLeapMonth' => $atts['is_leap_month'] !== '' ? filter_var( $atts['is_leap_month'], FILTER_VALIDATE_BOOLEAN ) : '',
			),
			static function ( $v ) {
				return $v !== '';
			}
		);
		$query = array_filter(
			array(
				'lang' => $atts['lang'],
			),
			static function ( $v ) {
				return $v !== '';
			}
		);
		$data = \RoxyAPI\Generated\Client::calculateLunarDate( $body, $query );

		if ( is_wp_error( $data ) ) {
			return \RoxyAPI\Support\Templates::api_error( $data );
		}

		return ComponentRenderer::render_atts( 'calculateLunarDate', is_array( $data ) ? $data : array(), $atts );
	}
}
