<?php
/**
 * Auto-generated hero shortcode: [roxy_crystals_by_zodiac]
 *
 * Crystals and stones associated with any zodiac sign, with properties, color, and chakra.
 *
 * Example: [roxy_crystals_by_zodiac sign="aries"]
 *
 * DO NOT EDIT. Generated by bin/generate.mjs from bin/hero-config.json.
 * To change this hero, edit the config and run: npm run generate
 *
 * @package RoxyAPI
 */

namespace RoxyAPI\Generated\Heroes;

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

class CrystalsByZodiac {

	/**
	 * Default attributes accepted by this shortcode. Single source of truth
	 * for the hero attribute contract test.
	 *
	 * @var array<string, string>
	 */
	public const DEFAULTS = array(
		'sign' => "",
		'mode' => 'auto',
	);

	/**
	 * Render the shortcode.
	 *
	 * @param array<string, string>|string $atts    Shortcode attributes.
	 * @param string                       $content Inner content.
	 * @param string                       $tag     Shortcode tag.
	 * @return string
	 */
	public static function render( $atts, $content = '', $tag = '' ): string {
		$atts = shortcode_atts(
			self::DEFAULTS,
			is_array( $atts ) ? $atts : array(),
			(string) $tag
		);

		wp_enqueue_style( 'roxyapi-frontend' );

		if ( $atts['mode'] === 'form' ) {
			return \RoxyAPI\Support\FormRenderer::render( \RoxyAPI\Generated\Forms\CrystalsByZodiacForm::class );
		}

		if ( $atts['sign'] === '' ) {
			if ( $atts['mode'] !== 'static' ) {
				return \RoxyAPI\Support\FormRenderer::render( \RoxyAPI\Generated\Forms\CrystalsByZodiacForm::class );
			}
			return \RoxyAPI\Support\Templates::error( sprintf( /* translators: %s is the canonical example shortcode. */ __( "The sign attribute is required. Example: %s", 'roxyapi' ), "[roxy_crystals_by_zodiac sign=\"aries\"]" ) );
		}

		$sign_clean = \RoxyAPI\Support\Sanitize::zodiac_sign( $atts['sign'] );

		$data = \RoxyAPI\Generated\Client::getCrystalsByZodiac( $sign_clean );

		if ( is_wp_error( $data ) ) {
			return \RoxyAPI\Support\Templates::api_error( $data );
		}

		return \RoxyAPI\Support\GenericRenderer::render( 'getCrystalsByZodiac', is_array( $data ) ? $data : array() );
	}

	/**
	 * Visitor-form data path. Same dispatch as render() but returns the raw
	 * API response (or a WP_Error) so the matching <Hero>Form::call() can
	 * surface it via the FormRouter PRG cycle. Caller must pass the form
	 * body keyed by the same attribute names as the shortcode accepts.
	 *
	 * @param array<string, mixed> $atts Form-body attributes.
	 * @return array<string, mixed>|\WP_Error
	 */
	public static function fetch_for_form( array $atts ) {
		$atts = array_merge( self::DEFAULTS, $atts );

		if ( $atts['sign'] === '' ) {
			return new \WP_Error( 'roxyapi_missing_attrs', sprintf( /* translators: %s is the canonical example shortcode. */ __( "The sign attribute is required. Example: %s", 'roxyapi' ), "[roxy_crystals_by_zodiac sign=\"aries\"]" ) );
		}

		$sign_clean = \RoxyAPI\Support\Sanitize::zodiac_sign( $atts['sign'] );

		$data = \RoxyAPI\Generated\Client::getCrystalsByZodiac( $sign_clean );

		if ( is_wp_error( $data ) ) {
			return $data;
		}

		return is_array( $data ) ? $data : array();
	}
}
