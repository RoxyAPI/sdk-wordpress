<?php
/**
 * Auto-generated hero shortcode: [roxy_life_path]
 *
 * Life path number and core meaning from a birth date.
 *
 * Example: [roxy_life_path birth_date="1990-05-15"]
 *
 * DO NOT EDIT. Generated by bin/generate.mjs from bin/hero-config.json.
 * To change this hero, edit the config and run: npm run generate
 *
 * @package RoxyAPI
 */

namespace RoxyAPI\Generated\Heroes;

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

class LifePath {

	/**
	 * Default attributes accepted by this shortcode. Single source of truth
	 * for the hero attribute contract test.
	 *
	 * @var array<string, string>
	 */
	public const DEFAULTS = array(
		'birth_date' => "",
	);

	/**
	 * Render the shortcode.
	 *
	 * @param array<string, string>|string $atts    Shortcode attributes.
	 * @param string                       $content Inner content.
	 * @param string                       $tag     Shortcode tag.
	 * @return string
	 */
	public static function render( $atts, $content = '', $tag = '' ): string {
		$atts = shortcode_atts(
			self::DEFAULTS,
			is_array( $atts ) ? $atts : array(),
			(string) $tag
		);

		wp_enqueue_style( 'roxyapi-frontend' );

		if ( $atts['birth_date'] === '' ) {
			return \RoxyAPI\Support\Templates::error( sprintf( /* translators: %s is the canonical example shortcode. */ __( "The birth_date attribute is required. Example: %s", 'roxyapi' ), "[roxy_life_path birth_date=\"1990-05-15\"]" ) );
		}

		$birth_date_parts = \RoxyAPI\Support\HeroTransforms::split_iso_date_into_year_month_day( $atts['birth_date'] );

		if ( $birth_date_parts === null ) {
			return \RoxyAPI\Support\Templates::error( sprintf( /* translators: %s is the canonical example shortcode. */ __( "The birth_date attribute must be in YYYY-MM-DD format. Example: %s", 'roxyapi' ), "[roxy_life_path birth_date=\"1990-05-15\"]" ) );
		}

		$data = \RoxyAPI\Generated\Client::calculateLifePath( array(
			'year' => $birth_date_parts['year'],
			'month' => $birth_date_parts['month'],
			'day' => $birth_date_parts['day'],
		) );

		if ( is_wp_error( $data ) ) {
			return \RoxyAPI\Support\Templates::api_error( $data );
		}

		return \RoxyAPI\Support\GenericRenderer::render( 'calculateLifePath', is_array( $data ) ? $data : array() );
	}
}
